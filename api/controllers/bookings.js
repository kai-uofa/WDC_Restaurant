const db = require('../models/dbconnection');

const Bookings = {
  getAllBookings(req, res) {
    // TODO: get all ACTIVE bookings from database
    //
  },

  getBookingDetails(req, res) {},

  setBookingStatus(req, res) {},
};

module.exports = Bookings;
